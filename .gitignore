/.forge/
/build/
/tmp/
.envrc

# forge-factory materialises the versions in go.mod. It is committed so the
# module runs with a bare go run, and sync keeps it fresh.

# forge-factory materialises these. A version is written in forge-factory.yaml.
/.envrc
